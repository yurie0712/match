<section class="sct-color-1 slice">
  <div class="container">
    <div class="row">
      <div class="col-md-12 post_images_container">
        <div class="post-item">
          <div class="post-meta-top">
            <div class="post-image">
                <%= attachment_image_tag @post, :image, width: "500px" %>
            </div>
          </div>
          <!-- 投稿情報 -->
          <div class="post-content">
            <ul class="meta-info-cells v4 float-wrapper">
              <!-- ユーザー情報 -->
              <li>
                <%= link_to user_path(@post.user) do %>
                  <%= @post.user.name %>
                <% end %>
              </li>
              <!-- ↑ここまで -->
              <!-- 投稿日 -->
              <li>
                <%= @post.created_at.strftime('%Y/%m/%d') %>
              </li>
              <!-- ↑ここまで -->
              <!-- いいね -->
              <li>
                <div id="likes_buttons_<%= @post.id %>">
                  <%= render partial: 'likes/likebutton', locals: { post: @post} %>
                </div>
              </li>
              <!-- ↑ここまで -->
              <!-- 編集／削除ボタン -->
              <li>
                <% if @post.user == current_user %>
                  <%= link_to "編集", edit_post_path(@post) %>
                <% end %>
              </li>
              <li>
                <% if @post.user == current_user %>
                  <%= link_to "削除", post_path(@post), method: :delete %>
                <% end %>
              </li>
              <!-- ↑ここまで -->
            </ul>
          </div>
          <!-- caption情報 -->
          <div class="comment-list bt style-2">
            <ol>
              <li class="comment">
                <div class="comment-body bb">
                  <div class="comment-text">
                    <div class="comment-entry"><%= render_with_post_tags(@post.caption) %></div>
                  </div>
                </div>
              </li>
            </ol>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>